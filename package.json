{
  "name": "bani-forlor-fake-api",
  "main": "server.js",
  "scripts": {
    "serve": "node generate-data.js && json-server --watch database.json --port=3000"
  },
  "dependencies": {
    "@faker-js/faker": "^7.6.0"
  }
}
